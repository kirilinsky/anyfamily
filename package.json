{
  "name": "anyfamily",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for the any* family — micro, zero-dependency Intl tools.",
  "homepage": "https://anyfamily.site",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kirilinsky/anyfamily.git"
  },
  "author": "Kirilinsky (https://github.com/kirilinsky)",
  "license": "MIT",
  "scripts": {
    "versions": "node scripts/versions.mjs",
    "predev": "pnpm run build:packages && node scripts/versions.mjs",
    "prebuild": "pnpm run build:packages && node scripts/versions.mjs",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "changeset": "changeset",
    "version-packages": "changeset version && node scripts/sync-jsr.mjs && pnpm install --lockfile-only",
    "release": "pnpm run build:packages && changeset publish",
    "build:packages": "pnpm --filter \"./packages/*\" build"
  },
  "dependencies": {
    "@dateforge/react-calendar": "^1.6.0",
    "@vercel/analytics": "^2.0.1",
    "anyamount": "workspace:*",
    "anyaround": "workspace:*",
    "anylocale": "workspace:*",
    "anylong": "workspace:*",
    "anymany": "workspace:*",
    "anyplural": "workspace:*",
    "anywhen": "workspace:*",
    "anyword": "workspace:*",
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
