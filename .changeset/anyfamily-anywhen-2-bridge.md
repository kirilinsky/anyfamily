---
"anyfamily": minor
---

Tracks anywhen 2.0. `anywhen` is re-exported from its new single-export shape,
and `anywhenParts` stays available as a deprecated alias for `anywhen.parts`, so
nothing breaks for anyfamily users while the rest of the family migrates.

Every bridge like this is removed at once in anyfamily 2.0, when all seven
packages are on the new shape.
