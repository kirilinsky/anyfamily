---
"anyfamily": patch
---

Re-exports the new `AnywhenRangeOptions` and `AnyamountParseOptions` types; `anywhen.range`, `anyamount.range` and `anyamount.parse` come through the function objects as before.
